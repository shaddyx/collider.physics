class Mesh2D{
    constructor(){
        this.vectorArray = [];
    }
    /**
     * 
     * @param {Mesh2D} mesh
     * @returns {Vector2D|undefined}
     */
    intersects(mesh){
        for (let k in this.vectorArray){
            
        }
    }
}