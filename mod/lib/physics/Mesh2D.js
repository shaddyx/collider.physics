class Mesh2D{
    constructor(){
        this.vectorArray = [];
    }
    /**
     * 
     * @param {Mesh2D} mesh 
     */
    intersects(mesh){
        for (let k in this.vectorArray){
            
        }
    }
}